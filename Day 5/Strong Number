def factorial(n):
    if n == 0:
        return 1
    else:
        return n * factorial(n-1)
num = 145
sum_factorials = sum([factorial(int(digit)) for digit in str(num)]) 
if sum_factorials == num:
    print(f"{num} is a strong number.")
else:
    print(f"{num} is not a strong number.")

OUTPUT:
145 is a strong number.
